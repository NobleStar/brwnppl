%script#sharer_template{type: "text/x-handlebars-template"}
  .userPhoto
    %img#photo{ :src => user_avatar(current_user) }

  .shareBox
    .sayBulb

    .clear

    %form#shareForm
      %a{:href=> '/help/guidelines', :style => 'float: right;', :target => '_blank'}= "Please be mindful of our Community Guidelines"
      %textarea.linkBar.hidden
        = "Give your story a title. ;)"

      = select_tag "community", options_from_collection_for_select(Community.all, "id", "name"), :prompt => '-- Please Select Category --', :class => 'hidden linkBar'

      = select_tag "content_type_dropdown", options_from_collection_for_select(ContentType.all, :internal_name, :public_name), :prompt => '-- Please Select Content Type --', :class => 'hidden linkBar'

      %input.linkBar{:type => 'text', :placeholder => 'Please enter your link here or type a topic for discussion..'}

      %center
        %img#linkBarImage.hidden{ :src => '' }

      %center
        #preview

      %input#story_type{ type: :hidden, name: :story_type, value: 'WebLink'}
      %input#story_image{ type: :hidden, name: :story_image_display}

      %a{ :href => '#', :id => 'image_upload' } Attach Image
      %input#fileupload.hidden{ :type => 'file', :name => 'image', 'data-url' => "/api/images/upload" }

      %img.loader.hidden{ :src => '/assets/loader.gif' }

  .shareButton
    %a#shareThis{ :href => '' }
      Share
    %input.hidden{ :type => 'hidden', name: :story_image }

    .clear